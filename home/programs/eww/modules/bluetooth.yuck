(defwidget bluetooth []
  (button
    :class "module-bt module icon"
    :onclick "blueberry"
    :tooltip "${bluetooth.text} ${bluetooth.batt_icon}"
    :style "color: ${bluetooth.color};"
    {bluetooth.icon}))
