(defwidget cpu []
  (circular-progress
    :value "${EWW_CPU.avg}"
    :class "cpubar module"
    :thickness 3
    (button
      :tooltip "using ${round(EWW_CPU.avg,0)}% cpu"
      :onclick "./scripts/pop system"
      (label :class "icon-text" :text ""))))
