(defwidget bluetooth []
  (button
    :class "module-bt module"
    :onclick "blueberry"
    :tooltip "${bluetooth.text} ${bluetooth.batt_icon}"
    :style "color: ${bluetooth.color};"
    {bluetooth.icon}))
